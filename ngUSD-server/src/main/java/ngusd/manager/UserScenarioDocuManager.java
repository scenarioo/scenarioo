package ngusd.manager;

/**
 * The manager manages the calculation of additional data on the filesystem.
 * 
 * The manager runs a spearate thread to calculate this data and store it on
 * file system for later faster access. Once the data is calculated the old step
 * files are removed.
 * 
 * The manager ensures that the same information is not calculated more than
 * once.
 * 
 */
public class UserScenarioDocuManager {
	
}
